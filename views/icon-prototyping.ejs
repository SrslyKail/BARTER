<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Icon Prototyping</title>
</head>

<body>
   <style>
      * {
         margin: 0;
         padding: 0;
      }

      #testingGrid {
         display: grid;
         gap:2em;
         grid-template-columns: repeat(2, 125px);
         grid-auto-rows: 110px;
         grid-auto-flow: row;
      }

      .cardImageIcon {
         display: inline-flex;
         align-items: center;
         width:100%;
      }
      svg {
         position: absolute;
      }
      .cardTextDiv {
         display: flex;
         position: relative;
         background-color: #17191595;
         align-items: center; /*needed with higher text background height*/
         height:min-content;  /*adjusts text background height*/
         width: inherit;
      }
      .iconText {
         color: #FFFAEE;
         width: inherit;
         font-size: x-large;
         text-align: center;
      }
   </style>

   <div id=testingGrid>

   <!--Sets image location, mostly for testing, change image:image in include to work through an array-->
   <%var image = "/wow.jpeg"%>
      <!--Sets text, mostly for testing, change text:text for live-->
   <%var text = "Wow!"%>

      <!--text set in for loop just sends the current number  !!set text:null for no text!!-->
   <% let i=0; %>
	<% for(i=0; i < 185; i++) { %>
      <%text = i;%> 
         <%- include('templates/card-image-icon', {image:image, text:text})%>
   <% } %> 

   
   </div>

</body>

</html>