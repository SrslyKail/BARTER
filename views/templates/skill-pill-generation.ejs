  <!-- Instructions: 
    Use this to create groups of skill pills

    include('templates/card-pill-generation, {db:db, count:count}

    use DB to send a database to be iterated through
    **this isn't implemented currently****ask Ben if you need it** use count to determine a maximum number of icons, set to null for full db 
    takes a parent page parameter to format the generated link on an icon, 
      exclude if you don't want a link
      folder format (/foldername)
    
    Instructions for skill-pill-box:
    Takes image and text parameters to generate pill boxes
    Set text=null for no-image pills
    Find mandatory CSS and options in cardIconStyle.css
    Template for no text + no image: 
    <%#- include('templates/components/skill-pill-box', { image:"/wow.jpeg", folder:null, text:null}) %>
    -->
  <div id="centering">
    <div id="carousel">
      <div class="pillGrid">
        <!--A loop for generating pills, 
            loop works with grid settings in cardIconStyle.css
            needs integration testing with bootstrap-->
        <% for (let i = 0; i < db.length; i ++) {  %>
        <%- include('components/skill-pill-box', {image:db[i].image, text:db[i].name, count:i}) %>
        <% } %>
      </div>
    </div>
  </div>