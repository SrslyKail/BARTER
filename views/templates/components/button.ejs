<div class="d-grid align-items-center">
  <button class="btn btn-dark btn-rounded btn-large" type="<%= buttonType %>" role="button" <%= typeof disabled !== 'undefined' && disabled ? 'disabled' : '' %>>
    <%= buttonText %>
  </button>
</div>
