<%- include("templates/header") %>

<!-- po:
/profile/username(UPD: IMPLEMENTED AS /profile?id=username SHOULD BE SUFFICIENT FOR MVP) should be how any user's profile is accessed. I THINK
todo if this is the profile of said user, they should have the ability to edit whatever.
-->
<div class="mx-1"><%- include("templates/components/profileCard")%></div>
<section class="p-4 text-center mb-4">
    <h2><b>Skills</b></h2>
    <% for (let i = 0; i < skills.length; i ++) {  %>
        <a href="/" style="color:black;" class="fs-4 my-4"><u><%=skills[i]%></u></a><br/>
        <%} %>
    <hr class="my-4">
    <h2><b>Reviews</b></h2>
</section>

<%- include("templates/footer", {footerButton: include("templates/components/footerButton", {buttonText: "CONTACT", email: email}) }) %>